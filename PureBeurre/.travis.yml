dist: bionic
language: python
python:
        - '3.6.9'
addons:
        firefox: latest
branches:
        only:
                - staging

before_script:
        - install-webdrivers
        - pip install -r requirements.txt
env:
        global:
                - DJANGO_SETTINGS_MODULE="config.settings.travis"

services:
        - postgresql

script:
        - python manage.py test -v2
